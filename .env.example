# Supabase project settings (Project Settings → API)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Shared admin password for /admin.html
ADMIN_PASSWORD=

# Random long string used to sign admin session cookies (e.g. generate with `openssl rand -hex 32`)
ADMIN_SESSION_SECRET=
